// Sintaxe:
// condição ? valor_se_verdadeiro : valor_se_falso;

const usuario = null;

const mensagem = usuario ? `Bem-vindo, ${usuario}!`: "Bem-vindo, visitante!";

console.log(mensagem); // Bem-vindo, visitante!
