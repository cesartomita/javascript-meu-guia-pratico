// A conversão acontece automaticamente pelo runtime (no caso de linguagens interpretadas como JavaScript) ou pelo compilador (em linguagens compiladas).
// Isso ocorre quando você mistura tipos de dados diferentes em expressões e o JavaScript ou outra linguagem tenta realizar a conversão automaticamente para que a operação seja válida.

var num1 = "10";
var num2 = 5;
var soma = num1 + num2;

console.log(soma); // 105